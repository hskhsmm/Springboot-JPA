spring:
  thymeleaf:
    prefix: classpath:/templates/
    suffix: .html

  datasource:
    url: jdbc:h2:tcp://localhost/C:/Users/user/Desktop/studyDB/h2_prac/mydb
    driver-class-name: org.h2.Driver
    username: sa       # ?? ?? (????? ??)
    password:          # ????? ??? ??

  h2:
    console:
      enabled: true

  jpa:
    hibernate:
      ddl-auto: update   # ?? create, validate ? ??? ??
    show-sql: true

org:
  hibernate:
    orm:
      jdbc:
        bind:
          debug